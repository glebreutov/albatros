const positionOk = {
  ack: true,
  error: {...}
}

const newOrder = {
  ack: true,
  id: '4534ercs',
  pair : {base: 'USDT', counter: 'BTC'},
  error: {...}
}

const waitForExec = {
  ack: true,
  error: {...}
}

const cancel = {
  ack: true,
  error: {...}
}

const transferFunds = {
  ack: true,
  error: {...}
}

const closePos = {
  ack: true,
  error: {...}
}

const transferStatus = {
  ack: true,
  beforeSrc: 10,
  afterSrc: 1000,
  beforeTgt: 10,
  afterTgt: 1000,
  error: {...}
}